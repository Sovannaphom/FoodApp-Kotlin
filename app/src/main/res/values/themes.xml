<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <item name="colorPrimary" type="color">@color/colorPrimary</item>
    <item name="colorPrimaryVariant" type="color">@color/accent</item>

    <style name="Theme.FoodApp" parent="Base.Theme.FoodApp" />
</resources>